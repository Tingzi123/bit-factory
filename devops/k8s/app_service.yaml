apiVersion: v1
kind: Service
metadata:
  name: service-clusterip
  namespace: dev
spec:
  selector:
    app: bitfactory
  clusterIP: 10.97.97.97 # service的ip地址，如果不写，默认会生成一个
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080  # Service端口
      nodePort: 32082